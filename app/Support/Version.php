<?php

// The installed application version. Bumped once per release as part of
// the release process (see docs/project-documentation.md) — never
// inferred from git metadata at runtime, since a deployed release on a
// customer's server has no .git directory.

const GARAGEOS_VERSION = '1.0.0';
